# set(BINARY ${CMAKE_PROJECT_NAME}_perft)

# file(GLOB_RECURSE SOURCES LIST_DIRECTORIES true *.h *.cpp)

# set(SOURCES ${SOURCES})

# add_executable(${BINARY} ${SOURCES})

# target_link_libraries(${BINARY} PUBLIC ${CMAKE_PROJECT_NAME}_lib)
